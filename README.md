# Support Bot - Telegram бот для поддержки

Telegram бот для организации системы поддержки с использованием форумов (топиков) в группах.

## 🚀 Быстрая установка

### Windows

```powershell
# Клонируйте репозиторий
git clone https://github.com/ваш-username/support-bot.git
cd support-bot

# Запустите установщик
powershell -ExecutionPolicy Bypass -File install.ps1
```

### Linux / Mac

```bash
# Клонируйте репозиторий
git clone https://github.com/ваш-username/support-bot.git
cd support-bot

# Запустите установщик
bash install.sh
```

## 📋 Ручная установка

Если установщик не работает, выполните шаги вручную:

1. **Установите Python 3.8+**
   - Windows: https://www.python.org/downloads/
   - Linux: `sudo apt install python3 python3-pip` (Ubuntu/Debian)
   - Mac: `brew install python3`

2. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте конфигурацию**
   ```bash
   cp .env.example .env
   # Откройте .env и заполните необходимые данные
   ```

## ⚙️ Настройка

Откройте файл `.env` и заполните следующие параметры:

### BOT_TOKEN
Токен вашего бота от [@BotFather](https://t.me/BotFather)

1. Напишите `/newbot` боту @BotFather
2. Следуйте инструкциям
3. Скопируйте полученный токен в `.env`

### SUPPORT_CHAT_ID
ID группы/канала для поддержки (должен быть форумом с топиками)

1. Создайте группу в Telegram
2. Включите режим "Форум" в настройках группы
3. Добавьте бота в группу как администратора
4. Напишите боту команду `/get_chat_id` в группе
5. Скопируйте полученный Chat ID в `.env`

### ADMIN_IDS
ID администраторов (через запятую, без пробелов)

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID
3. Вставьте в `.env` (можно несколько через запятую: `123456789,987654321`)

### DB_PATH (опционально)
Путь к базе данных. По умолчанию: `bot/bot.db`

## 🏃 Запуск

### Обычный запуск

```bash
python bot.py
```

или

```bash
python3 bot.py
```

### Запуск через Docker

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 📖 Основные функции

- ✅ Система тикетов через форумы Telegram
- ✅ FAQ (база знаний) с поддержкой медиа
- ✅ Антиспам защита
- ✅ Бан/разбан пользователей
- ✅ Админ-панель для управления
- ✅ Поддержка альбомов (несколько фото/видео)
- ✅ Ответы на сообщения (reply)

## 🎮 Команды для операторов

В группе поддержки доступны команды:

- `/ban [причина]` - Заблокировать пользователя
- `/unban [ID]` - Разблокировать пользователя
- `/close` - Закрыть тикет
- `/check` - Спросить "Могу помочь?"
- `/faq` - Отправить ответ из FAQ
- `/checkban USER_ID` - Проверить статус бана

## 🔧 Админ-панель

Администраторы могут использовать команду `/admin` в личных сообщениях с ботом для доступа к панели управления:

- Редактирование главного меню
- Управление FAQ
- Настройка панели управления
- Настройка контакта для жалоб

## 📁 Структура проекта

```
support-bot/
├── bot.py              # Основной файл бота
├── bot/                # Директория с базой данных
│   └── bot.db         # База данных SQLite
├── requirements.txt    # Зависимости Python
├── .env               # Конфигурация (создается при установке)
├── .env.example       # Шаблон конфигурации
├── Dockerfile         # Docker образ
├── docker-compose.yml # Docker Compose конфигурация
├── install.sh         # Установщик для Linux/Mac
├── install.ps1        # Установщик для Windows
└── README.md          # Документация
```

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что токен в `.env` правильный
- Убедитесь, что бот запущен
- Проверьте логи на наличие ошибок

### Ошибка "SUPPORT_CHAT_ID не задан"
- Убедитесь, что группа является форумом
- Проверьте, что бот добавлен в группу как администратор
- Используйте `/get_chat_id` для получения правильного ID

### Ошибка при установке зависимостей
- Обновите pip: `pip install --upgrade pip`
- Используйте виртуальное окружение:
  ```bash
  python -m venv venv
  source venv/bin/activate  # Linux/Mac
  venv\Scripts\activate    # Windows
  pip install -r requirements.txt
  ```

## 📝 Лицензия

Этот проект распространяется свободно. Используйте на свой страх и риск.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Решение проблем"
2. Создайте Issue в репозитории
3. Обратитесь к документации Telegram Bot API

## 🔄 Обновление

Для обновления бота:

```bash
# Остановите бота (Ctrl+C или docker-compose down)

# Обновите код
git pull

# Обновите зависимости
pip install -r requirements.txt --upgrade

# Запустите снова
python bot.py
```

---

**Приятного использования! 🎉**

