# Support Bot

Telegram бот для поддержки пользователей с системой тикетов, FAQ и админ-панелью.

## 🚀 Быстрая установка

### Автоматическая установка (одна команда):

```bash
curl -sSL https://raw.githubusercontent.com/mdeadice/support-bot/main/install.sh | sudo bash
```

После установки:

1. **Настройте бота:**
   ```bash
   sudo nano /opt/support-bot/.env
   ```

2. **Запустите бота:**
   ```bash
   cd /opt/support-bot && sudo docker-compose up -d
   ```

3. **Проверьте логи:**
   ```bash
   cd /opt/support-bot && sudo docker-compose logs -f
   ```

## 📋 Подготовка перед установкой

### 1. Получение токена бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Создание Forum группы для поддержки

1. Создайте новую группу в Telegram
2. **ВАЖНО:** Перейдите в настройки группы → выберите **"Темы"** (включить)
3. Выберите **"Список"** - это обязательно!
4. Добавьте вашего бота в группу как администратора
5. Дайте боту права на управление темами

### 3. Получение ID группы

1. Добавьте бота [@getidsbot](https://t.me/getidsbot) в вашу Forum группу
2. Отправьте в группе команду `/start` или любое сообщение
3. Бот ответит с ID группы (выглядит как `-1001234567890` - отрицательное число)
4. Скопируйте этот ID

### 4. Получение вашего Admin ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID (выглядит как `123456789` - положительное число)

## ⚙️ Настройка .env файла

После установки отредактируйте файл `/opt/support-bot/.env`:

```bash
sudo nano /opt/support-bot/.env
```

Укажите ваши данные:

```env
BOT_TOKEN=ваш_токен_от_BotFather
SUPPORT_CHAT_ID=ваш_id_группы_поддержки
ADMIN_IDS=ваш_telegram_id
DB_PATH=/app/bot.db
```

**Пример:**
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
SUPPORT_CHAT_ID=-1001234567890
ADMIN_IDS=123456789
DB_PATH=/app/bot.db
```

Если администраторов несколько, укажите ID через запятую:
```env
ADMIN_IDS=123456789,987654321,555666777
```

## 🔄 Обновление

### Автоматическое обновление (одна команда):

```bash
curl -sSL https://raw.githubusercontent.com/mdeadice/support-bot/main/update.sh | sudo bash
```

**Важно:** База данных автоматически сохраняется перед обновлением. Старая БД не удаляется.

## 📍 Расположение файлов

Все файлы бота находятся в `/opt/support-bot/`:

- `bot/bot.py` - основной файл бота
- `bot/bot.db` - база данных (сохраняется при обновлении)
- `.env` - файл с настройками
- `docker-compose.yml` - конфигурация Docker
- `Dockerfile` - образ контейнера

## 🎮 Использование

### Команды для пользователей

- `/start` - Главное меню

### Команды для операторов (в группе поддержки)

- `/ban [причина]` - Заблокировать пользователя
- `/unban [ID]` - Разблокировать пользователя
- `/close` - Закрыть тикет
- `/check` - Отправить вопрос "Могу помочь?"
- `/faq` - Отправить ответ из FAQ

### Команды для администраторов

- `/admin` - Админ-панель (в личных сообщениях)

## 🛠 Управление ботом

```bash
# Переход в директорию бота
cd /opt/support-bot

# Просмотр логов
sudo docker-compose logs -f

# Остановка
sudo docker-compose down

# Запуск
sudo docker-compose up -d

# Перезапуск
sudo docker-compose restart

# Обновление
curl -sSL https://raw.githubusercontent.com/mdeadice/support-bot/main/bot/update.sh | sudo bash
```

## ⚠️ Важные замечания

1. **Forum группа обязательна** - бот работает только с Forum группами (не обычными группами или супергруппами)
2. **Темы должны быть включены** - в настройках группы должен быть включен "Список тем"
3. **Бот должен быть администратором** - добавьте бота в группу как администратора с правами на создание тем
4. **База данных сохраняется** - при обновлении старая БД не удаляется, создается резервная копия

## 🔧 Требования

- Linux VPS/VM
- Docker
- docker-compose
- Python 3.11+ (устанавливается автоматически в контейнере)

## 📝 Переменные окружения

- `BOT_TOKEN` - Токен бота от @BotFather
- `SUPPORT_CHAT_ID` - ID Forum группы поддержки (отрицательное число)
- `ADMIN_IDS` - ID администраторов через запятую
- `DB_PATH` - Путь к базе данных (по умолчанию `/app/bot.db`)

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `cd /opt/support-bot && sudo docker-compose logs -f`
2. Убедитесь, что группа имеет тип "Forum" и темы включены
3. Проверьте, что бот добавлен как администратор в группу
4. Убедитесь, что все данные в `.env` указаны правильно

## Лицензия

MIT

